# Server configuration
PORT=3000
NODE_ENV=development

# Admin API Key (REQUIRED for cache management)
# Generate a secure random key: openssl rand -hex 32
ADMIN_API_KEY=your-secure-secret-key-here

# Optional: Set in production
# NODE_ENV=production
